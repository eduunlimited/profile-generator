# OpenAI — used for name/street misspell jigs only
OPENAI_API_KEY=sk-...
# Optional override (defaults to gpt-4o-mini)
# VITE_OPENAI_MODEL=gpt-4o-mini

# Browser-only Vite dev fallback (optional if OPENAI_API_KEY is set for the Vite server)
# VITE_OPENAI_API_KEY=sk-...
